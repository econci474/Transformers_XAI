# Integration arm: the repo holds CODE only. Fusion result artifacts (tables,
# per-seed metrics, predictions, failure plots, extracted-prob CSVs, etc.) are
# regenerated by the scripts and live here un-versioned (same policy as outputs/).
*.png
*.csv
*.npz
*.parquet
*.tex
__pycache__/
